import "../../styles/auth.css";

/** Auth route group — split-pane screens ported from designs/auth/. Design
 *  tokens come from styles/tokens.css (imported by the root layout, which
 *  wraps this group); this stylesheet only loads the auth surfaces. */
export default function AuthLayout({ children }: { children: React.ReactNode }) {
  return children;
}
