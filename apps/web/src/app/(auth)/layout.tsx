import "./auth.css";

/** Auth route group — split-pane screens ported from designs/auth/. The
 *  stylesheet above is the design's own token set and classes, imported
 *  here so it only loads on auth routes. */
export default function AuthLayout({ children }: { children: React.ReactNode }) {
  return children;
}
