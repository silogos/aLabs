"use client";

/** Bare board — /{orgSlug}/{projectSlug}/tasks. The board itself renders
 * from the tasks layout; the drawer (if any) is the [taskId] child page. */
export default function TasksPage() {
  return null;
}
