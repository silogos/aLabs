"use client";

/** Bare directory — /{orgSlug}/members. The list itself renders from the
 *  (list) layout; the member drawer (if any) is the [userId] child page. */
export default function MembersPage() {
  return null;
}
